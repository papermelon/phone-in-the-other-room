# Campfire character profiles — local validation, 20 September 2026

Implements the [founder’s revised profile contract](../../../docs/plans/campfire-profiles-2026-09-20.md): consistent Campfire terminology, established character names, one Global disclosure, detailed profiles, tappable activity bubbles and numbered channels with eight participants each. The profile contains current tasks/routines, session times/state, intention, account-attributed recorded history, party names and Farm inventory/appearance. Party-only sharing retains its earlier scope. Version-1 public agreements do not acquire broader access silently.

## Validation

- `xcodegen generate` succeeded after source membership changed; no new target, package, entitlement or signing setting was introduced.
- `xcodebuild build -project PhoneInTheOtherRoom.xcodeproj -scheme PhoneInTheOtherRoom -destination 'generic/platform=iOS Simulator'` — passed on final source, including channels and the publication-hint retry identity. Log: `/tmp/campfire-channels-final-build.log`.
- `xcodebuild test -project PhoneInTheOtherRoom.xcodeproj -scheme PhoneInTheOtherRoom -destination 'platform=iOS Simulator,id=A0DB65B8-F3FC-4961-8B0C-9689F51C9EA1' -parallel-testing-enabled NO` — **1,100 tests, zero failures on final source**. Result: `/tmp/campfire-channels-final-tests-20260920.xcresult`; log: `/tmp/campfire-channels-final-tests.log`. Includes legacy-state compatibility for optional channel metadata and known-owner profile checks. Earlier simulator startup stalls were interrupted and retried; a test that referenced app-only models was corrected to exercise the shared owner rule used by the profile builder.
- `node --experimental-strip-types --test supabase/tests/global_campfire_validation.mjs` — **8 tests passed**. Includes versioned disclosure, bounded schema, character names, detail addressing, channel inputs and malformed/extra fields.
- `deno check supabase/functions/campfire-global/index.ts` — passed using the cached Deno executable.
- The additive `20260920120000_campfire_profiles.sql` migration applied transactionally to an isolated local PostgreSQL 17 copy of the empty synthetic test database. The channel revision used `/tmp/campfire-channel-pg` on local socket port 55452; no production database was contacted. `campfire_profiles_test.sql`, `campfire_channels_test.sql` and `global_campfire_test.sql` passed with rollback-only fixtures. Coverage includes v1/v2 receipt separation, verified character name, global and party detail reads, a mismatched source snapshot, block/withdrawal/termination fencing, pagination, idempotency, retention and service-only access. Channel tests verify an eight-person cap, automatic allocation, full-channel rejection, moving to a freed seat, filters/blocks preserving occupancy, source fencing, and near-future session reservations. A separate 24-connection synthetic admission check produced exactly three occupied channels of eight and one empty channel (`/tmp/campfire-channel-race.py`). Logs: `/tmp/campfire-channel-sql.log`. The existing regression suite’s single `cron.job` installation assertion was excluded in a temporary test copy because this local PostgreSQL has no pg_cron extension; actual pruning logic passed. Production scheduling was not tested or changed.
- `git diff --check` passed. No commit, push, backend deployment or app distribution was performed. The isolated SQL server was stopped after verification and its synthetic users were removed.

## UI inspection

Disposable native fixtures use release components and isolated defaults. The visibility sheet was inspected with the existing character name and no alias picker (`visibility-dark.png`). Earlier eight-person scene inspection exposed clipping and overlapping names; bubbles now use a compact single line and larger gatherings have more vertical space. Full details remain reachable by tapping the seat or accessible list row. The shared Farm renderer hides customization/play controls and permits browsing its pages. Final channel/spacing and read-only Farm captures could not be completed: CoreSimulator launch attempts stalled or returned `FBSOpenApplicationServiceErrorDomain`, and disposable simulator boot status remained in CoreLocation data migration. The affected QA simulators and this task’s pending launch commands were stopped. These captures and small-phone maximum Dynamic Type remain unverified, not passed. `scene-before-spacing-fix.png` records the earlier overlap finding, not the final layout.

VoiceOver labels/actions and selected-state semantics were inspected in source; spoken VoiceOver, physical tap/hold/drag, two-account network convergence and shielding behavior remain device acceptance work. Farm previews reconstruct shared sheep/equipment/character appearance, not another phone’s transient animations or locally moved positions.

## Rollout

The migration and matching Edge revision are **local only**. The previously activated alias-only Global slice does not provide these profile APIs. The native choice checks `profileVersion == 2` before offering the wider share. Backend deployment, updated native distribution and physical two-account acceptance remain separate authorized actions.
