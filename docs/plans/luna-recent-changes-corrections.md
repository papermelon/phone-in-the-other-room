# Correction prompt — existing Luna implementation task

Prepared 2026-08-27 for **Implement Home Phone Away handoff**, task ID `01a04229-4396-7362-8429-66dff77bc4bf`, host `local`. Use the existing task with Luna / Extra High. This packet is prepared only; no message has been sent.

## Prompt

Implement corrections from `docs/plans/recent-changes-sol-review.md`, findings F1–F10. Read that report, AGENTS.md, the original Home/Phone Away handoff and accepted direction before editing. Sol's verdict is **fix-first** despite both 664-test suites and the 19 Deno tests passing. Several defects are inherited from the Aug 25–26 release work; do not limit corrections to your last patch.

First confirm the actual candidate: `/Users/ngawangchime/Desktop/Developer Projects/Phone in the Other Room`, branch `codex/night-flock-mvp`, HEAD `cce766d1840a850a23d5c47e4a542203c26a728b`, plus its inherited and implementation dirty/untracked files. Preserve all of them. Snapshot the new starting state; do not reset to HEAD or assume another checkout contains this work. Sol's evidence is in `/tmp/counting-sheep-sol-review-20260827/`; your earlier pre-edit snapshot is `/tmp/counting-sheep-home-phone-away-preedit.EGiLPm`.

Implement in this order:

1. **Session ownership and terminal integration (F1/F2/F4).** A scheduled ordinary morning must not hide a running session. Finishing the ordinary morning must not leave its parent barrier running. Final start admission must reject a run/morning that became active while confirmation or NFC was pending, before consuming a source or publishing/scheduling side effects. Preserve explicit early-wake defer/skip choices, existing NFC/emergency authorization, independent settlement and receipt priority. Check ordinary early exit before wake so it is not accidentally treated as explicit defer.
2. **Supported protection scheduling (F3).** Handle Apple's minimum monitoring interval without lengthening the actual consented barrier. Verify the five-minute practice and every offered Phone Away duration, including the 15-minute boundary and a saved window with little time remaining. If retaining a supported short-session experience requires a product/API decision you cannot safely resolve, explain that exact decision to the founder. Do not silently remove practice, weaken the required protection contract, or mask a failed monitor with a “ready” label. The 30-minute default is not evidence for a new product maximum.
3. **Durable outbox (F5).** Acknowledge exactly the sent status/source identity so a newer queued revision survives an older response. Cover asynchronous interleavings and restart, retaining account-generation fencing and server idempotency. Do not change backend state or deploy anything.
4. **Purpose and Active content (F6/F7).** Refresh the observable purpose after real occurrence/revision/epoch transitions, not polling. Keep it private and separate from future preferences. Show the actual selected evening/morning optional sequence in the proper primary phase; keep overnight and Phone Away free of bedtime checklists or source discovery. Cover natural morning transitions and return/restore.
5. **Finish guidance/Home (F8/F9/F10).** Make guidance occasional by an explicit opportunity/display policy, consistent with persisted dismissal. Deep-link a displayed idea to its own detail. Add only honest routine mappings; do not turn caffeine timing into preparing a bag. Wire draft versus saved-routine contexts correctly. Fix the 164-point mascot inside a 54-point wrapper and verify text/layout on smaller phones and accessibility sizes. Keep the personal ritual primary and exactly one prominent Slumber Party bridge.

Use existing tests/fixtures first. Add coverage at the real coordinator/view-model/service boundaries where the current Shared-only tests miss integration. The existing Screenbook active fixture directly assigns a run and does not create the ordinary morning, so it cannot prove a real start is correct. Do not resolve findings by changing only fixtures or expected assertions. Do not build an unrelated UI-test architecture without explaining what is needed.

Follow AGENTS.md: no hand-editing generated project files, no new dependencies, and ask before changing project.yml, targets, entitlements, signing or tab structure. Do not commit, push, merge, deploy, or upload. Do not erase simulator/user data. Preserve unknown or concurrent work.

Validation must include XcodeGen, generic simulator build, full unit suite, the SlumberPartyQA validation/build/test lane, and relevant Deno checks. Report actual command exit statuses. Use an available simulator if iPhone 15 is absent. Capture real Home and Active UI, and explicitly label any Family Controls/NFC/Apple account/Watch/overnight checks that still require a physical phone. Do not count fixture renders as interaction proof.

Finish by updating `docs/plans/home-phone-away-implementation-report.md` with exact cwd/HEAD/status, per-finding resolution and evidence, new versus inherited changes, actual test results/artifact paths, screenshots, and unresolved decisions/device gates. Do not overwrite Sol's review. Return to the founder for another Sol review of the resulting candidate; passing the old tests alone does not change the verdict.
